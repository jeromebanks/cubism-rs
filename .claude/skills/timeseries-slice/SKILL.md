---
name: timeseries-slice
description: Retired Cubism time-series delivery workflow. Use only to redirect an older timeseries-slice request to the canonical work-slice process; never execute the former shared-branch process.
---

# Retired

The shared time-series branch merged in PR #24. Do not recreate, resume, or push
to it. All future time-series work follows `.claude/skills/work-slice/SKILL.md`.

Historical rationale remains in `docs/sdlc-process-notes.md` and the
`docs/TIMESERIES_*_HANDOFF.md` files. Read one only when a current slice's
`Context` section links it.
